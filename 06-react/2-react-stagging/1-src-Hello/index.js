// 引入React核心库
import React from 'react';
// 引入ReactDOM
import ReactDOM from 'react-dom';
// 引入App组件（App是所有组件的外壳组件，它包着所有的组件）
import App from './App';

// 渲染App组件
ReactDOM.render(<App/>, document.getElementById('root'));
